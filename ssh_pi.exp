#!/usr/bin/expect -f
set timeout 10
set password "quackattack"
set user "pi"
set host "192.168.86.31"
set cmd [lindex $argv 0]

spawn ssh -o StrictHostKeyChecking=no $user@$host $cmd
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
